export class product {
  constructor(id, name, backCamera, desc, frontCamera, img, price, screen, type) {
    this.id = id;
    this.name = name;
    this.backCamera = backCamera;
    this.desc = desc;
    this.frontCamera = frontCamera;
    this.img = img;
    this.price = price;
    this.screen = screen;
    this.type = type;
  }
}
